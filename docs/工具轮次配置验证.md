# CX 工具轮次配置验证报告

> 验证时间：2026-06-03
> 验证人：CX
> 验证目标：确认代码任务工具轮次是否为 15

---

## 一、配置文件分析

### 1.1 cx-listener.mjs（第 355-356 行）

```javascript
const toolRounds = (isCodeTask || isHardTask) ? 15 : 5;
console.log(`[CX] 工具轮次: ${toolRounds}次 (${isCodeTask ? '代码' : isHardTask ? '困难' : '日常'}任务)`);
```

**结论：代码任务工具轮次 = 15**

### 1.2 任务类型识别（cx-listener.mjs）

| 行号 | 代码 | 说明 |
|------|------|------|
| 123-124 | `CODE_TASK: /@CX\s*\[代码\]\s*(.+)/i`<br>`HARD_TASK: /@CX\s*\[困难\]\s*(.+)/i` | 消息协议正则匹配 |
| 308-309 | `const isCodeTask = MSG_PROTOCOL.CODE_TASK.test(content)`<br>`const isHardTask = MSG_PROTOCOL.HARD_TASK.test(content)` | 任务类型判断 |

### 1.3 ai-reply.js（第 599 行）

```javascript
const maxIterations = maxToolRounds || parseInt(process.env.MAX_TOOL_ROUNDS) || 12;
```

**默认值：12 轮（可被参数覆盖）**

---

## 二、工具轮次策略表

| 任务类型 | 触发标签 | 工具轮次 | 识别正则 |
|---------|---------|---------|---------|
| **代码任务** | `@CX [代码]` | **15 次** | `/@CX\s*\[代码\]\s*(.+)/i` |
| **困难任务** | `@CX [困难]` | **15 次** | `/@CX\s*\[困难\]\s*(.+)/i` |
| 日常任务 | `@CX [日常]` | 5 次 | 默认 |

---

## 三、验证结果

| 检查项 | 预期值 | 实际值 | 状态 |
|--------|--------|--------|------|
| 代码任务工具轮次 | 15 | 15 | ✅ 通过 |
| 日常任务工具轮次 | 5 | 5 | ✅ 通过 |
| 任务类型识别 | 正则匹配 | 正则匹配 | ✅ 通过 |

---

## 四、相关文件路径

| 文件 | 路径 |
|------|------|
| CX Listener | `D:/BKS/projects/team-workspace/src/workers/cx-listener.mjs` |
| AI Reply SDK | `D:/BKS/projects/team-workspace/src/sdk/ai-reply.js` |

---

## 五、备注

1. **当前配置满足需求**：代码任务已正确配置为 15 次工具轮次
2. **降级链已配置**：代码任务使用 SiliconFlow DeepSeek V4 Pro，失败后自动降级到智谱 GLM-4.7
3. **超时策略仍为 60s**：建议按之前讨论改为自适应超时（日常 30s，代码 120s）