# 任务汇报

**任务**: 请详细列出当前项目的所有文件，并分析每个文件的作用和依赖关系。
**时间**: 2026-06-01T07:36:34.057Z
**执行者**: CC

## 详细内容

@小马 [完成] 项目文件清单及依赖分析 | D:\BKS\projects\team-workspace | T:匹配 O:合规 K:有效

---

**项目文件完整清单及分析：**

**一、根目录配置文件**

| 文件 | 作用 | 依赖 |
|------|------|------|
| `package.json` | 项目配置、依赖声明 | 无 |
| `vite.config.js` | Vite 构建配置 | 无 |
| `index.html` | 入口 HTML | src/main.jsx |
| `CLAUDE.md` | Claude Code 配置 | 无 |

**二、前端源码（src/）**

| 文件 | 作用 | 依赖 |
|------|------|------|
| `main.jsx` | 入口文件 | App.jsx |
| `App.jsx` | 主组件 | PixelOffice, ChatPanel, StatusBar, ws/client |
| `components/PixelOffice/PixelOffice.jsx` | 像素办公室面板 | grids/CharacterGrids.js |
| `components/ChatPanel/ChatPanel.jsx` | 聊天面板 | ChatInput, MessageItem |
| `components/ChatPanel/ChatInput.jsx` | 消息输入框 | 无 |
| `components/ChatPanel/MessageItem.jsx` | 消息项渲染 | 无 |
| `components/StatusBar/StatusBar.jsx` | 状态栏 | 无 |
| `grids/CharacterGrids.js` | 角色像素网格数据 | 无 |
| `ws/client.js` | WebSocket 客户端 | 无 |
| `sdk/agent-client.js` | Agent SDK 核心 | 无 |
| `sdk/memory.js` | 记忆管理 | 无 |
| `sdk/shared-memory.js` | 共享记忆 | 无 |
| `sdk/ai-reply.js` | AI 回复逻辑 | 无 |
| `sdk/cache.js` | 缓存管理 | 无 |

**三、后端源码（server/）**

| 文件 | 作用 | 依赖 |
|------|------|------|
| `index.js` | 服务器入口 | db.js, routes/*, ws/handler |
| `db.js` | SQLite 数据库初始化 | 无 |
| `ws/handler.js` | WebSocket 处理器 | db.js |
| `routes/message.js` | 消息路由 | db.js |
| `routes/history.js` | 历史记录路由 | db.js |
| `routes/register.js` | Agent 注册路由 | db.js |
| `routes/status.js` | 状态路由 | db.js |
| `routes/heartbeat.js` | 心跳路由 | db.js |

**四、启动脚本**

| 文件 | 作用 |
|------|------|
| `start-cc.mjs` | CC Agent 启动 |
| `start-xiaoma.mjs` | 小马 Agent 启动 |
| `start-xiaoma-local.mjs` | 小马本地启动 |
| `start-xiaoma-poller.mjs` | 小马轮询启动 |
| `test-sdk.mjs` | SDK 测试 |
| `send-to-chat.mjs` | 消息发送工具 |

**五、核心依赖链路**

```
index.html → main.jsx → App.jsx
                          ├── PixelOffice.jsx → CharacterGrids.js
                          ├── ChatPanel.jsx → ChatInput.jsx + MessageItem.jsx
                          ├── StatusBar.jsx
                          └── ws/client.js ↔ server/ws/handler.js ↔ db.js
```

**六、数据流向**

```
Agent (start-cc/xiaoma) → HTTP POST → server/routes/* → db.js → SQLite
                                      ↓
                              ws/handler.js → WebSocket → 前端 ws/client.js → UI 更新
```

---
*自动生成，定期清理*