# 任务汇报

**任务**: 请详细列出当前项目的所有文件，并分析每个文件的作用和依赖关系。
**时间**: 2026-06-01T07:36:47.185Z
**执行者**: CC

## 详细内容

<tool_call>
<function=write_file>
<parameter=path>D:/BKS/projects/team-workspace/docs/项目文件清单与依赖分析.md</parameter>
<parameter=content># 项目文件清单与依赖分析

> **文档版本：** v1.0
> **编写人：** CC（研发部）
> **日期：** 2026-06-01

---

## 1. 项目概述

**项目名称：** Team Workspace（团队工作室）
**技术栈：** Preact + Vite（前端）| Express + WebSocket + sql.js（后端）
**端口：** 开发模式前端 :5173 / 后端 :3210 | 生产模式 :3210

---

## 2. 完整文件清单

### 2.1 根目录配置文件

| 文件 | 作用 | 依赖 |
|------|------|------|
| `package.json` | 项目依赖配置、脚本定义 | 无 |
| `package-lock.json` | 依赖版本锁定 | package.json |
| `vite.config.js` | Vite 构建配置、代理规则 | @preact/preset-vite |
| `index.html` | 前端入口 HTML | main.jsx |
| `CLAUDE.md` | Claude Code 项目记忆 | 无 |
| `README.md` | 项目说明文档 | 无 |
| `.gitignore` | Git 忽略规则 | 无 |

### 2.2 启动脚本

| 文件 | 作用 | 依赖 |
|------|------|------|
| `start-cc.mjs` | CC Agent 启动（Hub-Spoke 模式）| server/index.js |
| `start-xiaoma.mjs` | 小马 Agent 启动 | server/index.js |
| `start-xiaoma-poller.mjs` | 小马轮询启动 | server/index.js |
| `start-xiaoma-poller.bat` | Windows 批处理启动轮询 | start-xiaoma-poller.mjs |
| `start-xiaoma-local.bat` | Windows 批处理本地启动 | start-xiaoma-local.mjs |
| `start-xiaoma-local.mjs` | 小马本地模式启动 | server/index.js |
| `setup-local-model.bat` | 本地模型配置脚本 | 无 |
| `send-to-chat.mjs` | 消息发送工具 | server API |
| `send-to-chat.sh` | Shell 版消息发送 | server API |
| `test-sdk.mjs` | Agent SDK 测试 | src/sdk/agent-client.js |
| `test-local-model.mjs` | 本地模型测试 | 无 |
| `test-local-model.bat` | Windows 批处理模型测试 | test-local-model.mjs |

### 2.3 前端源码 (src/)

#### 2.3.1 入口文件

| 文件 | 作用 | 依赖 |
|------|------|------|
| `main.jsx` | 前端入口，挂载 App 组件 | preact, App.jsx |
| `App.jsx` | 根组件，组合所有子组件 | PixelOffice, ChatPanel, StatusBar |

#### 2.3.2 组件 (src/components/)

| 目录/文件 | 作用 | 依赖 |
|-----------|------|------|
| `PixelOffice/PixelOffice.jsx` | 像素办公室主场景 | CharacterGrids.js |
| `PixelOffice/PixelOffice.css` | 像素办公室样式 | 无 |
| `ChatPanel/ChatPanel.jsx` | 群聊主面板 | MessageItem, ChatInput |
| `ChatPanel/ChatInput.jsx` | 消息输入框 | 无 |
| `ChatPanel/MessageItem.jsx` | 单条消息渲染 | marked, dompurify |
| `StatusBar/StatusBar.jsx` | 状态栏组件 | 无 |

#### 2.3.3 像素网格 (src/grids/)

| 文件 | 作用 | 依赖 |
|------|------|------|
| `CharacterGrids.js` | 角色像素网格数据（Clawd/小马）| 无 |

#### 2.3.4 Agent SDK (src/sdk/)

| 文件 | 作用 | 依赖 |
|------|------|------|
| `agent-client.js` | Agent 接入 SDK 封装 | 无 |
| `ai-reply.js` | AI 回复处理 | 无 |
| `cache.js` | 消息缓存机制 | 无 |
| `memory.js` | Agent 记忆管理 | 无 |
| `shared-memory.js` | 共享记忆系统 | 无 |
| `examples/cc-agent.js` | CC Agent 示例 | agent-client.js |
| `examples/cc-listener.mjs` | CC 监听器（Node 版）| agent-client.js |
| `examples/xiaoma-agent.js` | 小马 Agent 示例 | agent-client.js |
| `examples/xiaoma-agent.mjs` | 小马 Agent（Node 版）| agent-client.js |
| `examples/xiaoma-listener.mjs` | 小马监听器 | agent-client.js |
| `examples/xiaoma-poller.mjs` | 小马轮询器 | agent-client.js |

#### 2.3.5 WebSocket 客户端 (src/ws/)

| 文件 | 作用 | 依赖 |
|------|------|------|
| `client.js` | WebSocket 客户端封装 | 无 |

#### 2.3.6 状态管理 (src/store/)

| 文件 | 作用 | 依赖 |
|------|------|------|
| （空目录） | 预留状态管理 | 无 |

#### 2.3.7 样式文件 (src/styles/)

| 文件 | 作用 | 依赖 |
|------|------|------|
| `global.css` | 全局样式 | 无 |
| `chat.css` | 聊天面板样式 | 无 |
| `office.css` | 像素办公室样式 | 无 |
| `status.css` | 状态栏样式 | 无 |

### 2.4 后端源码 (server/)

| 文件 | 作用 | 依赖 |
|------|------|------|
| `index.js` | 后端入口，初始化 Express/WebSocket | express, ws, db.js, 所有路由 |
| `db.js` | sql.js 数据库初始化 | sql.js |
| `ws/handler.js` | WebSocket 消息处理 | 无 |

#### 2.4.1 API 路由 (server/routes/)

| 文件 | 作用 | 依赖 |
|------|------|------|
| `status.js` | 状态上报接口 | db.js |
| `message.js` | 消息收发接口 | db.js, ws/handler.js |
| `heartbeat.js` | 心跳保活接口 | db.js |
| `register.js` | Agent 注册接口 | db.js |
| `history.js` | 历史消息查询 | db.js |

### 2.5 静态资源 (public/)

| 文件 | 作用 |
|------|------|
| `favicon.ico` | 网站图标 |
| `assets/agent.webp` | Agent 头像 |
| `assets/cc_sprite.png` | CC 像素精灵图 |
| `assets/clawd.png` | Clawd 螃蟹图片 |
| `fonts/` | 字体文件目录 |

### 2.6 项目文档 (docs/)

| 文件 | 作用 |
|------|------|
| `PRD_团队工作室.md` | 产品需求文档 |
| `技术方案_团队工作室.md` | 技术方案文档 |
| `技术决策_前端框架选型.md` | 框架选型决策 |
| `原型_像素工作室.html` | 可交互原型 |
| `设计参考_frontend-design-skill.md` | 前端设计参考 |

### 2.7 数据目录 (data/)

| 文件 | 作用 |
|------|------|
| `workspace.db` | SQLite 数据库文件 |

### 2.8 构建产物 (dist/)

| 文件 | 作用 |
|------|------|
| `index.html` | 构建后的入口 |
| `assets/` | 构建后的静态资源 |
| `fonts/` | 构建后的字体 |

---

## 3. 依赖关系图

```
┌─────────────────────────────────────────────────────────────┐
│                    浏览器 (localhost:3210)                    │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐           │
│  │ PixelOffice  │ │  ChatPanel   │ │  StatusBar   │           │
│  └──────┬──────┘ └──────┬──────┘ └──────┬──────┘           │
│         │               │               │                   │
│         └───────────────┼───────────────┘                   │
│                         ▼                                    │
│                    App.jsx (根组件)                           │
│                         │                                    │
│                    main.jsx (入口)                            │
└─────────────────────────────────────────────────────────────┘

---
*自动生成，定期清理*